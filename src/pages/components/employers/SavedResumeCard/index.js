import React from 'react';
import { Divider, Stack } from '@mui/material';

const SavedResumeCard = () => {
  return <Stack sx={{ mb: 3 }}>
     <Divider />
    
  </Stack>;
};

export default SavedResumeCard;
